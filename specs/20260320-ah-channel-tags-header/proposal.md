# Ah-Channel-Tags Header

Add a `Ah-Channel-Tags: a,b,c` header support, so that incoming requests will be sent to channels filtered by the specified tags. This filtering is applied after the current profile configuration.
